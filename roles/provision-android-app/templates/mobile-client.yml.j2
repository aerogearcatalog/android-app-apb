apiVersion: mobile.k8s.io/v1alpha1
kind: MobileClient
metadata:
    annotations:
        icon: fa fa-android
        service_instance_name: {{ service_instance_name.stdout }}
    name: {{ appName }}-android
    namespace: {{ namespace }}
spec:
    apiKey: {{ service_instance_name | to_uuid}}
    appIdentifier: {{ appIdentifier }}
    clientType: android
    excludedServices: null
    name: {{ appName }}